<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :city_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :city_description %>
  </div>

  <div class="field">
    <%= form.label :small_image %>
    <%= form.text_field :small_image, id: :city_small_image %>
  </div>

  <div class="field">
    <%= form.label :zipcode %>
    <%= form.text_field :zipcode, id: :city_zipcode %>
  </div>

  <div class="field">
    <%= form.label :external_url %>
    <%= form.text_field :external_url, id: :city_external_url %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :city_slug %>
  </div>

  <div class="field">
    <%= form.label :department_id %>
    <%= form.text_field :department_id, id: :city_department_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
